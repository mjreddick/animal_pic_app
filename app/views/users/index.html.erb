<h1>All Users</h1>

<ul>
	
	<% @users.each do |user| %>

		<li>
			<strong><%= user.username %></strong><%= link_to "More info", user_path(user) %>
		</li>

	<% end %>

</ul>

<%= link_to "new_user", new_user_path %>